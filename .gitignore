# Dev-only tooling — never shipped (D-19)
node_modules/
.eslintcache

# Local Claude Code config (incl. preview launch.json) — not a site artifact
.claude/

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# OS / editor cruft
.DS_Store
Thumbs.db
