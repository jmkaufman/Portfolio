---
name: task-t05-content-migration
description: Build task T-05 — author the real portfolio content into the semantic HTML
metadata:
  type: project
---

**Task T-05 — Content migration.** Place the real content into the HTML skeleton.

- **Scope:** Author name/bio, experience (Epsilon C5I — two Raytheon roles GPNTS & SSDS with dates/languages/tools), the Pong project (desc + live URL + repo + thumbnail), a derived Skills/Tech list, and contact links (mailto + LinkedIn + GitHub). Use the accept-duplication markup pattern for repeated blocks. Migrate content only — do NOT carry over old visual design.
- **Implements:** D-05 (content in HTML), D-28 (accept markup duplication), D-34 (clean redesign, content-only migration).
- **Depends on:** [[task-t04-html-skeleton]].
- **Artifacts:** content within `index.html`.
- **Content source:** the migration table in [`../IMPLEMENTATION_PLAN.md`](../IMPLEMENTATION_PLAN.md) (and the handoff prompt).
- **Open items the owner must supply:** GitHub profile URL (likely github.com/jmkaufman), whether to expand the sparse content (1 employer, 1 project), bio expansion. See [[portfolio-open-content-items]].
